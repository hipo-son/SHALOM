# =============================================================================
# SHALOM — Environment Variable Template
# =============================================================================
# 1. Copy this file:  cp .env.example .env
# 2. Fill in your keys below
# 3. Load in shell:   source .env        (bash/zsh)
#                     Get-Content .env | ForEach-Object { ... }  (PowerShell)
# =============================================================================

# ── LLM Provider (하나 이상 필요 / At least one required) ────────────────────
#
# OpenAI (GPT-4o 사용)
#   발급: https://platform.openai.com/api-keys
#   요금: 요청당 $0.01~0.10 수준 (소규모 작업 기준)
#
# export OPENAI_API_KEY=sk-your-openai-key-here

# Anthropic Claude (OpenAI 대신 사용 가능)
#   발급: https://console.anthropic.com/
#
# export ANTHROPIC_API_KEY=sk-ant-your-anthropic-key-here

# ── Materials Project (구조 조회 시 필요 / Required for structure lookup) ──────
#
# mp-19717, Fe2O3 같은 MP ID / 화학식으로 구조를 가져올 때 사용
#   발급: https://next-gen.materialsproject.org/api  (무료)
#
# export MP_API_KEY=your-materials-project-api-key-here

# ── Quantum ESPRESSO 슈도포텐셜 경로 (QE backend 사용 시) ──────────────────────
#
# SSSP Efficiency v1.3.0 UPF 파일이 저장된 디렉토리
# 미설정 시 현재 작업 디렉토리(./)를 사용
# 슈도포텐셜 다운로드: python -m shalom setup-qe --elements Si,Fe --download
#
# export SHALOM_PSEUDO_DIR=/home/username/pseudopotentials
# export SHALOM_PSEUDO_DIR=/c/Users/username/pseudopotentials  # Windows
