# =============================================================================
# SHALOM - Environment Variable Template
# =============================================================================
# Setup:
#   1. Copy this file:  cp .env.example .env
#   2. Replace placeholder values with your actual keys
#   3. Load in shell:   source .env           (Linux/macOS bash/zsh)
#      Windows:         set manually in System Environment Variables
#      PowerShell:      $env:MP_API_KEY = "your-key-here"
#
# NOTE: Lines starting with "export" are active. Comment out (#) to disable.
# =============================================================================

# -- Materials Project (free, optional) ---------------------------------------
# Enables fetching structures by MP ID (mp-19717) or formula (Fe2O3).
# Without this key, SHALOM uses built-in ASE structures for simple elements.
# Get your free key at: https://next-gen.materialsproject.org/api
#
export MP_API_KEY=your-materials-project-api-key-here

# -- LLM Provider (required only for 'pipeline' command) ---------------------
# The autonomous multi-agent pipeline needs one of these.
# Basic commands (run, workflow, converge, plot) do NOT need LLM keys.
#
# OpenAI (default): https://platform.openai.com/api-keys
# export OPENAI_API_KEY=sk-your-openai-key-here
#
# Anthropic Claude (alternative): https://console.anthropic.com/
# export ANTHROPIC_API_KEY=sk-ant-your-anthropic-key-here
#
# Local LLM (no key needed): set base_url instead
# export SHALOM_LLM_BASE_URL=http://localhost:11434/v1

# -- Workspace (where DFT output folders are created) ------------------------
# Default: ~/Desktop/shalom-runs (if Desktop exists) or ~/shalom-runs
# Override per-run with: --workspace/-w or --output/-o flags
#
# export SHALOM_WORKSPACE=~/Desktop/shalom-runs

# -- Quantum ESPRESSO pseudopotentials (for QE execution) --------------------
# Directory containing SSSP Efficiency v1.3.0 UPF files.
# Download them: python -m shalom setup-qe --elements Si,Fe,O --download
# Default: ~/pseudopotentials
#
# export SHALOM_PSEUDO_DIR=~/pseudopotentials

# -- Audit logging (optional) ------------------------------------------------
# export SHALOM_AUDIT_LOG=~/.shalom/audit.log
